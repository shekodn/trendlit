#!/usr/bin/python3

class Error(object):
    def __init__(self, code):
        self.code = code
        # self.message = message
    # def __str__(self):
    #     return "%d %s" % (self.code, self.error)
