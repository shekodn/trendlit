# Don't move, otherwise the TEST will break


program the_name_of_the_program

    script {
      int A
      int B
      int C
      int D = 1
      int E
      bool F
      str G
      double H
      eval(A)
      eval(1)
      eval(True)
      eval(A + B)
      eval(A > B)

    }
