program the_name_of_the_program

    script {
        int A
        int B
        eval(2>1)
        eval(A > 1)
        eval(1 is B)
        eval(10 + 20)
        eval(100 + A)
        eval(B + 200)
        eval("Falta generar este quadruplo") # TODO
    }
