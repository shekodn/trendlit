program the_name_of_the_program

      script {
      int A
      int B
      int C
      # eval((1+2*2) > (5 + 3/2))
      # eval(B+C/D*E-F*H)
      eval(A*(B+C))
      # eval(A*(B+C/D*E-F*H))
      # > C * (B + D / E))

      } # closes script
